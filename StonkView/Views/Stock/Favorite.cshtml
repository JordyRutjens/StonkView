@{
    ViewData["Title"] = "Favorites";
    var favorites = ViewData["FavoriteList"];
}
@model StonkView.Models.FavoriteModel


<br />
<body>
    <div class="container-fluid shadow p-3 mb-5 rounded">

        <div class="text-center">
            <h1 class="display-4">S&P 500</h1>
        </div>


        <p>
            <div class="input-container">
                <input type="text" id="SearchStock" placeholder="Search Stock" onkeyup="search()">
            </div>
        </p>

        <table class="table table-striped shadow p-3 mb-5 rounded" id="StockTable">
            <thead>
                <tr>
                    <th>
                        SYMBOL
                    </th>
                    <th>
                        TIME ADDED
                    </th>
                    <th>
                    </th>
                    <th>
                   </th>

                </tr>
            </thead>
            <tbody>
                @*@foreach (FavoriteModel favorite in favorites)
                {*@
                <tr id="SearchItem">
                    <td style="color:dodgerblue">
                        @*@Html.DisplayFor(modelItem => favorite.Symbol)*@
                    </td>
                    <td>
                        @*@Html.DisplayFor(modelItem => favorite.AddDate)*@
                    </td>

                    <td>
                        <a asp-action="RemoveFavorite" id="RemoveButton" class="btn">Remove</a>
                    </td>
                    <td>
                        <a asp-action="Details" id="DetailButton" class="btn">Details</a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</body>